- name: Append note
  lineinfile:
    path: "{{ ssh_sshd_config }}"
    line: '#Generated by ansible'

- block:
  - include_tasks: set_option.yml
    vars:
      ssh_option: "{{ item.option }}"
      ssh_value: "{{ item.value }}"
    loop:
      "{{ ssh_options }}"
  become: true

- name: Stop and Start ssh
  service:
    name: ssh
    state: restarted
